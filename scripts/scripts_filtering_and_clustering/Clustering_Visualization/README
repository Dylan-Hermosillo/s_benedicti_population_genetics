The EIGENVEC file output by the clustering scripts can be visualized using the R script here.
Input requirements are: the EIGENVEC file (to plot the PCA), the EIGENVAL file (to get percentage of variance), and a CSV (to enable color-coding)
The CSV is used to associate samples with specific attributes (e.g. coast, location, developmental type), and is kept here for ease of access.
